{
  "name": "pty-mcp",
  "version": "0.1.0",
  "type": "module",
  "description": "MCP server for headless terminal emulation — spawn PTY sessions, send keystrokes, capture screenshots",
  "license": "MIT",
  "keywords": ["mcp", "terminal", "pty", "headless", "xterm", "model-context-protocol"],
  "bin": {
    "pty-mcp": "src/cli.ts"
  },
  "files": ["src/cli.ts", "src/server.ts", "src/cast-tail.ts", "src/terminal.ts", "src/renderer.ts", "src/pty.ts", "src/runtime.ts", "assets/", "README.md"],
  "scripts": {
    "start": "bun src/server.ts",
    "dev": "bun --watch src/server.ts",
    "test": "bun vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "@xterm/headless": "^6.0.0",
    "@xterm/addon-serialize": "^0.14.0",
    "@napi-rs/canvas": "^0.1.94",
    "bun-pty": "^0.4.8",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "vitest": "^3.0.0"
  }
}
